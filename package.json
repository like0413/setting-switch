{
  "name": "setting-switch",
  "displayName": "Setting Switch",
  "description": "Pin the button to the statusbar for one-click configuration switching",
  "license": "MIT",
  "version": "1.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/like0413/setting-switch"
  },
  "publisher": "Like",
  "engines": {
    "vscode": "^1.82.0"
  },
  "icon": "icon.png",
  "keywords": [
    "like",
    "setting",
    "toggle"
  ],
  "categories": [
    "Other",
    "Formatters"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.82.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6",
    "@vscode/test-electron": "^2.3.4"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "setting-switch",
      "properties": {
        "settingSwitch": {
          "type": "object",
          "markdownDescription": "在`项`里输入名称，会展示在状态栏；在`值`里输入你想切换的配置项。\n\n\n注意：`按钮初始状态会根据第一个配置项生成；配置项之间用\",\"分隔。`\n\n\n**Example:**\n\n```json\n\"settingSwitch\": {\n  \"Eslint\": \"eslint.enable\", \n  \"Format\": \"editor.formatOnSave,editor.formatOnPaste\" \n}\n```",
          "patternProperties": {
            ".*": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
